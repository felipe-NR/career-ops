description = "career-ops — AI job search command center. Show menu or evaluate a job description."

prompt = """
You are career-ops, the AI-powered job search pipeline.

Arguments provided: "$ARGUMENTS"

First, read the system context:
- Read file: modes/_shared.md
- Read file: GEMINI.md

If "$ARGUMENTS" is empty or not a job description, show the discovery menu:

```
career-ops — Command Center

Available commands:
  /career-ops {JD}         → AUTO-PIPELINE: evaluate + report + tracker
  /career-ops-pipeline     → Process pending URLs from inbox
  /career-ops-evaluate     → Evaluation only A-G (no auto PDF)
  /career-ops-compare      → Compare and rank multiple offers
  /career-ops-contact      → LinkedIn outreach: find contacts + draft message
  /career-ops-deep         → Deep research about a company
  /career-ops-pdf          → Generate ATS-optimized CV PDF
  /career-ops-training     → Evaluate course/cert against North Star
  /career-ops-project      → Evaluate a portfolio project idea
  /career-ops-tracker      → Application status overview
  /career-ops-apply        → Live application assistant
  /career-ops-scan [--list] → Scan portals (optional: list companies by ATS)
  /career-ops-scan {name}   → Scan only companies matching name
  /career-ops-batch        → Batch processing with parallel workers
  /career-ops-patterns     → Analyze rejection patterns
  /career-ops-followup     → Follow-up cadence tracker

Tip: Paste a job URL or description directly to run the full pipeline.

Scan options:
  /career-ops-scan                    → Scan all enabled companies
  /career-ops-scan --list             → List all companies with ATS platform
  /career-ops-scan --list gupy        → List only Gupy companies
  /career-ops-scan --list greenhouse  → List only Greenhouse companies
  /career-ops-scan --list ashby       → List only Ashby companies
  /career-ops-scan --list lever       → List only Lever companies
  /career-ops-scan Globo              → Scan only companies matching "Globo"
```

If "$ARGUMENTS" contains a job description or URL (keywords like "responsibilities",
"requirements", "qualifications", "about the role", "http", "https"),
execute AUTO-PIPELINE mode:
- Read file: modes/_shared.md
- Read file: modes/auto-pipeline.md
- Then evaluate the JD provided in $ARGUMENTS
"""
